import 'package:flutter/material.dart';

class AddSpellBody extends StatelessWidget {
  const AddSpellBody({ Key? key }) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container(
      
    );
  }
}